function encodeText(text,size,alphabet) {
  var size10 = size * 12
	if (text.length > size10) {
    crs = 0
    crs = parargaph.0
    cur = [text.0.slice(0,1),text.slice(1,2),text.slice(2,3),text.slice(3,4),text.slice(4,5),text.slice(5,6),text.slice(6,7),text.slice(7,8),text.slice(8,9),text.slice(9,10)]
    var crs2 = cur.0

  } else {
    console.log("Error: The text is too big.")
  }

}
